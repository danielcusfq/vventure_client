//model that represents user timeline

class UserTimeline {
  String idTimeline;
  String idPerson;
  String detail;
  int position;

  UserTimeline(this.idTimeline, this.idPerson, this.detail, this.position);
}
