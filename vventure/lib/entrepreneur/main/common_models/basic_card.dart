//model for the basic card that contains basic information of the user

class BasicCardInfo {
  String id;
  String organization;
  String name;
  String last;
  String image;

  BasicCardInfo(this.id, this.organization, this.name, this.last, this.image);
}
